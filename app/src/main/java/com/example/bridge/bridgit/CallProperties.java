package com.example.bridge.bridgit;

public class CallProperties extends CallFeature {
    // This will be expanded when I implement PartnerCalls.cs
    public CallProperties(Call call, Constraint... constraints) {
        super(call, constraints);
    }
}
